spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:visual_spider2;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration

